# Следующие шаги после проверки SSL/TLS

## ✅ SSL/TLS режим правильный (Full)

Режим SSL/TLS установлен правильно на "Full". Теперь проверьте следующее:

## 1. Очистка кеша Cloudflare (ОБЯЗАТЕЛЬНО!)

1. В Cloudflare Dashboard перейдите в **Caching** → **Configuration**
2. Нажмите кнопку **"Purge Everything"** (Очистить всё)
3. Подтвердите очистку
4. **Важно:** Подождите 2-3 минуты после очистки

## 2. Проверка настроек кеширования

1. В Cloudflare Dashboard → **Caching** → **Configuration**
2. Проверьте следующие настройки:
   - **Caching Level:** должен быть "Standard"
   - **Browser Cache TTL:** лучше установить "Respect Existing Headers" или "4 hours"
   - **Always Online:** можно включить

## 3. Проверка деплоя в Netlify

1. Зайдите в Netlify Dashboard
2. Найдите проект `clarityminiapp` (или ваш проект)
3. Проверьте последний деплой:
   - Статус должен быть **"Published"** (зеленый)
   - Если статус другой - пересоберите проект

4. Проверьте, что файлы задеплоены:
   - В Netlify Dashboard → **Deploys** → выберите последний деплой
   - Проверьте, что файлы `index.html`, `assets/index.*.js`, `assets/index.*.css` присутствуют

## 4. Проверка прямого доступа к файлам

Попробуйте открыть эти URL напрямую в браузере:

- `https://clarityminiapp.netlify.app/` - должна открыться главная страница
- `https://clarityminiapp.netlify.app/assets/index.BKhVh7uA.js` - должен открыться JavaScript файл
- `https://clarityminiapp.netlify.app/assets/index.CPWsFjWM.css` - должен открыться CSS файл

**Если файлы открываются напрямую через Netlify, но не через Cloudflare:**
- Проблема точно в настройках Cloudflare
- Попробуйте временно отключить прокси (серое облако) и проверить

## 5. Временное отключение прокси (для диагностики)

Если ничего не помогает:

1. В Cloudflare Dashboard → **DNS** → **Records**
2. Для обеих записей (`claritybot.ru` и `www`) переключите оранжевое облако на **серое** (DNS only)
3. Подождите 2-3 минуты
4. Проверьте, открывается ли сайт через `claritybot.ru`
5. Если открывается - проблема в настройках прокси Cloudflare
6. Если не открывается - проблема в деплое Netlify или DNS

## 6. Проверка через curl (опционально)

Если у вас есть доступ к терминалу, проверьте:

```bash
# Проверка главной страницы
curl -I https://claritybot.ru/

# Проверка JavaScript файла
curl -I https://claritybot.ru/assets/index.BKhVh7uA.js

# Проверка CSS файла
curl -I https://claritybot.ru/assets/index.CPWsFjWM.css
```

Все должны возвращать `200 OK`. Если возвращают `404` или другую ошибку - проблема в деплое или путях.

## 7. Пересборка и деплой проекта

Если файлы не задеплоены или устарели:

```bash
# Пересоберите проект
npm run build

# Проверьте, что файлы созданы
ls -la dist/assets/

# Задеплойте
npm run deploy
```

Или если используете автоматический деплой через Git:
```bash
git add .
git commit -m "Fix: Rebuild with updated vite config"
git push
```

## Порядок действий (рекомендуемый)

1. ✅ **SSL/TLS режим** - уже проверен, правильный (Full)
2. 🔄 **Очистите кеш Cloudflare** - обязательно!
3. 🔍 **Проверьте деплой Netlify** - убедитесь, что последний деплой успешен
4. 🌐 **Проверьте прямой доступ** - откройте файлы через Netlify домен
5. ⏳ **Подождите 5-10 минут** после всех изменений
6. 🔄 **Проверьте в режиме инкогнито** - откройте claritybot.ru в режиме инкогнито

## Если проблема сохраняется

После выполнения всех шагов, если белый экран все еще есть:

1. Сохраните скриншоты:
   - Консоль браузера с ошибками
   - Network tab с запросами
   - Настройки Cloudflare (SSL/TLS, Caching)

2. Проверьте логи в Netlify Dashboard → Functions → Logs

3. Попробуйте временно отключить прокси Cloudflare (серое облако) и проверить, работает ли сайт напрямую

