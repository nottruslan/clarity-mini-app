# Clarity — Telegram Mini App

> Личное приложение для повседневной эффективности: задачи, привычки, финансы и языки в одном месте внутри Telegram.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск локального сервера

```bash
npm run dev
```

Приложение запустится на `http://localhost:5173`

---

## 📱 Тестирование в Telegram

### Шаг 1: Создание бота

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/newbot`
3. Укажи имя бота (например: `Clarity Bot`)
4. Укажи username (например: `clarity_personal_bot`)
5. Сохрани полученный **токен** (понадобится позже)

### Шаг 2: Создание Mini App

1. В [@BotFather](https://t.me/BotFather) отправь `/newapp`
2. Выбери своего бота из списка
3. Укажи название приложения: `Clarity`
4. Укажи описание: `Personal productivity app`
5. Загрузи иконку (512x512 PNG)
6. Загрузи GIF демо (опционально)
7. **Укажи URL приложения** — здесь нужен HTTPS URL

### Шаг 3: Получение HTTPS URL для локального тестирования

Telegram требует HTTPS для Mini Apps. Используй один из вариантов:

#### Вариант A: ngrok (рекомендуется)

```bash
# Установка (если еще не установлен)
brew install ngrok

# Запуск туннеля
ngrok http 5173
```

Скопируй HTTPS URL (например: `https://abc123.ngrok.io`) и укажи его в @BotFather

#### Вариант B: Cloudflare Tunnel

```bash
# Установка
brew install cloudflare/cloudflare/cloudflared

# Запуск туннеля
cloudflared tunnel --url http://localhost:5173
```

### Шаг 4: Указание URL в @BotFather

1. В [@BotFather](https://t.me/BotFather) отправь `/myapps`
2. Выбери свой бот → выбери свое приложение
3. Нажми **Edit Web App URL**
4. Укажи HTTPS URL из ngrok/cloudflare

### Шаг 5: Открытие приложения

1. Открой своего бота в Telegram
2. Нажми кнопку **Открыть приложение** (или отправь `/start`)
3. Приложение откроется в Telegram! 🎉

---

## 🏗️ Структура проекта

```
clarity/
├── RULES.md                          # ⚠️ Правила разработки (обязательно к прочтению)
├── README.md                         # Этот файл
├── package.json
├── tsconfig.json
├── vite.config.ts
├── index.html
└── src/
    ├── main.tsx                      # Entry point + Telegram SDK init
    ├── App.tsx                       # Главный компонент
    ├── index.css                     # Базовые стили
    ├── assets/
    │   └── welcome-animation.json    # Lottie анимация для Welcome
    ├── components/
    │   ├── Layout.tsx                # Layout с safe areas и viewport
    │   ├── Welcome.tsx               # Onboarding экран (показывается один раз)
    │   └── AppShell.tsx              # Основной shell (4 секции + табы)
    └── hooks/
        ├── useTelegramTheme.ts       # Синхронизация темы Telegram
        └── useOnboarding.ts          # Логика onboarding + CloudStorage
```

---

## 🎨 Особенности

### ✅ Что уже работает

- **Welcome экран** — показывается один раз при первом запуске
- **CloudStorage** — флаг "onboarding пройден" синхронизируется между устройствами
- **Темная/светлая тема** — автоматическая синхронизация с Telegram
- **Safe areas** — поддержка notch и home indicator
- **Viewport режимы** — корректная работа в compact/fullsize/fullscreen
- **4 секции** — заглушки для Задач, Привычек, Финансов, Языков
- **Tabbar навигация** — переключение между секциями без routing

### 🔜 Что нужно реализовать

- [ ] Секция "Задачи" — добавление, удаление, отметка выполненных
- [ ] Секция "Привычки" — трекер с календарем и статистикой
- [ ] Секция "Финансы" — доходы, расходы, категории, баланс
- [ ] Секция "Языки" — переход к другому mini app или встроенный функционал
- [ ] Хранение данных в CloudStorage
- [ ] Swipe навигация между секциями

---

## 📚 Технологии

- **React 18** — UI фреймворк
- **TypeScript** — типизация
- **Vite** — сборщик
- **@telegram-apps/sdk-react** — Telegram Mini Apps SDK
- **@telegram-apps/telegram-ui** — официальный UI Kit
- **lottie-react** — анимации

---

## 🛠️ Команды

```bash
# Разработка
npm run dev

# Проверка типов
npm run type-check

# Production сборка
npm run build

# Предпросмотр production сборки
npm run preview
```

---

## 🚢 Production деплой

### Требования

- HTTPS обязателен
- Статический хостинг (SPA)

### Рекомендуемые платформы

#### 1. Vercel (рекомендуется)

```bash
npm install -g vercel
vercel --prod
```

#### 2. Netlify

```bash
npm install -g netlify-cli
npm run build
netlify deploy --prod --dir=dist
```

#### 3. Cloudflare Pages

```bash
npm run build
# Загрузи папку dist через веб-интерфейс Cloudflare Pages
```

### После деплоя

1. Скопируй production URL (например: `https://clarity.vercel.app`)
2. В [@BotFather](https://t.me/BotFather) → `/myapps` → выбери бот → **Edit Web App URL**
3. Укажи production URL
4. Готово! 🎉

---

## 🐛 Troubleshooting

### Приложение не открывается в Telegram

- ✅ Проверь, что URL начинается с `https://`
- ✅ Проверь, что сервер доступен (ngrok/cloudflare туннель запущен)
- ✅ Проверь консоль браузера в Telegram (Dev Tools)

### CloudStorage не работает

- ✅ CloudStorage доступен только внутри Telegram (не работает в браузере)
- ✅ Проверь, что SDK правильно инициализирован в `main.tsx`

### Неправильные цвета темы

- ✅ Проверь, что `useTelegramTheme` хук используется в `App.tsx`
- ✅ Проверь CSS переменные в DevTools

### Viewport не расширяется

- ✅ Проверь, что `viewport.expand()` вызывается в `main.tsx`
- ✅ Проверь режим открытия в Telegram (compact/fullsize)

---

## 📖 Документация

- [Telegram Web Apps API](https://core.telegram.org/bots/webapps)
- [Telegram Mini Apps Documentation](https://docs.telegram-mini-apps.com/)
- [TelegramUI Components](https://github.com/telegram-mini-apps-dev/TelegramUI)
- [tma.js SDK](https://github.com/Telegram-Mini-Apps/tma.js)

---

## ⚠️ Важно

**Перед любыми изменениями прочитай [`RULES.md`](./RULES.md)**

Этот файл содержит обязательные правила разработки Telegram Mini Apps:
- Запреты (page routing, кастомные кнопки, веб-паттерны)
- Требования (TelegramUI компоненты, viewport, safe areas)
- Архитектура и best practices

---

## 📝 Лицензия

MIT

---

**Сделано с ❤️ для продуктивности в Telegram**

