# Правила разработки Clarity — Telegram Mini App

> **ВАЖНО:** Этот документ содержит обязательные правила разработки.  
> Все изменения в проекте ДОЛЖНЫ соответствовать этим требованиям.

---

## 🎯 Главное правило

**Это Telegram Mini App, НЕ веб-сайт.**

Все решения должны следовать философии мобильного приложения внутри Telegram, а не традиционного веб-приложения.

---

## 📚 Официальная документация

**ОБЯЗАТЕЛЬНО изучить перед началом разработки:**

1. **Telegram Web Apps API**  
   https://core.telegram.org/bots/webapps  
   Основная документация по Web Apps

2. **Telegram Mini Apps Documentation**  
   https://docs.telegram-mini-apps.com/platform/creating-new-app  
   Полное руководство по созданию Mini Apps

3. **TelegramUI (UI Kit)**  
   https://github.com/telegram-mini-apps-dev/TelegramUI  
   Официальный UI Kit — использовать ТОЛЬКО его компоненты

4. **tma.js (SDK)**  
   https://github.com/Telegram-Mini-Apps/tma.js  
   JavaScript SDK для работы с Telegram Mini Apps

5. **Awesome Telegram Mini Apps**  
   https://github.com/telegram-mini-apps-dev/awesome-telegram-mini-apps?tab=readme-ov-file#ui-kits  
   Примеры, библиотеки и лучшие практики

6. **Telegram Bots API**  
   https://core.telegram.org/bots  
   Документация по созданию и настройке ботов

**При любых вопросах — сначала проверяй эти ресурсы.**

---

## 🚫 Строгие запреты

### 1. Никакого Page Routing
- ❌ Запрещено использовать `react-router`, `react-router-dom`, Next.js routing
- ❌ Запрещено использовать URL-based навигацию
- ✅ Навигация через состояние компонентов (`useState`)
- ✅ Slide-based или swipe-based переходы между экранами

### 2. Никаких кастомных кнопок "назад" и "закрыть"
- ❌ Запрещено добавлять свои кнопки закрытия/навигации назад
- ✅ Использовать только встроенные возможности Telegram (BackButton API)

### 3. Никаких веб-паттернов
- ❌ Запрещено делать традиционные header/footer как на сайтах
- ❌ Запрещено использовать классический скролл страницы
- ✅ Fullscreen canvas на весь экран
- ✅ Контент внутри экранов может скроллиться

---

## ✅ Обязательные требования

### 1. UI Kit
- **Только TelegramUI компоненты** из `@telegram-apps/telegram-ui`
- Запрещено использовать MUI, Ant Design, Bootstrap и прочие UI библиотеки
- Если нужен компонент, которого нет в TelegramUI — спросить разрешение

### 2. SDK Integration
- Использовать `@telegram-apps/sdk-react`
- Правильно инициализировать SDK:
  ```typescript
  const [miniApp] = initMiniApp();
  const [viewport] = initViewport();
  miniApp.ready();
  viewport.expand();
  ```

### 3. Viewport и Safe Areas
- Fullscreen mobile-first подход
- Использовать `100dvh` (dynamic viewport height)
- Учитывать `safe-area-inset-top` и `safe-area-inset-bottom`
- Слушать изменения viewport через SDK

### 4. Поддержка режимов окна
Приложение ДОЛЖНО корректно работать во всех режимах:
- **Compact** — минимальная высота
- **Fullsize** — стандартная высота
- **Fullscreen** — на весь экран

Слушать события:
```typescript
viewport.on('change:height', handleResize);
viewport.on('change:isExpanded', handleExpand);
```

### 5. Тема Telegram
- Синхронизировать light/dark тему с системной темой Telegram
- Слушать `themeParams.on('change', ...)`
- Использовать CSS переменные из Telegram:
  - `--tg-theme-bg-color`
  - `--tg-theme-text-color`
  - `--tg-theme-button-color`
  - и другие

### 6. Системные шрифты
Использовать шрифты из Telegram:
```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
```

---

## 🎨 Дизайн и UX

### 1. Навигация
- Slide-based или swipe-based переходы между секциями
- Tabbar внизу экрана (из TelegramUI)
- Плавные анимации (CSS transitions/transforms)

### 2. Layout
- Один главный экран на весь viewport
- Внутри экрана может быть scroll
- Но НЕ скролл всей страницы как на сайте

### 3. Компоненты
Использовать только из TelegramUI:
- `AppRoot` — корневая обертка
- `Tabbar` — нижняя навигация
- `Cell`, `Section` — группировка контента
- `Button`, `Input` — элементы форм
- `Placeholder` — пустые состояния
- `Title`, `Caption` — текст

---

## 🔧 Технический стек

### Обязательные технологии
- **Vite** — сборщик
- **React 18** — UI фреймворк
- **TypeScript** — типизация
- **@telegram-apps/sdk-react** — Telegram SDK
- **@telegram-apps/telegram-ui** — UI Kit

### Запрещенные технологии
- ❌ Next.js (это для SSR сайтов)
- ❌ React Router (page routing запрещен)
- ❌ Любые другие UI библиотеки кроме TelegramUI

---

## 📁 Структура проекта

```
clarity/
├── RULES.md                    # Этот файл
├── README.md                   # Инструкции для пользователя
├── package.json
├── tsconfig.json
├── vite.config.ts
├── index.html
└── src/
    ├── main.tsx               # Entry point с инициализацией SDK
    ├── App.tsx                # Главный компонент
    ├── index.css              # Базовые стили
    ├── components/            # React компоненты
    ├── hooks/                 # Кастомные хуки
    ├── storage/               # Работа с Telegram Cloud Storage
    └── types/                 # TypeScript типы
```

---

## 💾 Хранение данных

- Использовать **Telegram Cloud Storage** через SDK
- Данные автоматически синхронизируются между устройствами
- Не требуется свой backend для базовых функций

```typescript
import { cloudStorage } from '@telegram-apps/sdk-react';

// Сохранить
await cloudStorage.set('key', 'value');

// Получить
const value = await cloudStorage.get('key');
```

---

## 🚀 Процесс разработки

### Перед началом работы
1. Прочитать этот файл (RULES.md)
2. Изучить официальную документацию (см. раздел "📚 Официальная документация" выше)
3. Особое внимание уделить:
   - https://core.telegram.org/bots/webapps
   - https://docs.telegram-mini-apps.com/
   - https://github.com/telegram-mini-apps-dev/TelegramUI

### При добавлении новой функции
1. Убедиться, что не нарушаются правила
2. Использовать только TelegramUI компоненты
3. Тестировать на мобильном устройстве в Telegram

### При возникновении вопросов
- Проверить документацию Telegram Mini Apps
- Проверить примеры из официального репозитория
- Спросить разрешение, если нужно отступить от правил

---

## ✨ Философия

**Приложение должно ощущаться как встроенная функция Telegram:**
- Быстро загружается
- Плавно работает
- Выглядит нативно
- Использует возможности платформы

**Пользователь НЕ должен чувствовать, что это веб-сайт.**

---

## 🎯 Цель проекта

Clarity — это личное приложение для повседневной эффективности:
- Задачи (to-do)
- Привычки (habit tracker)
- Финансы (доходы/расходы)
- Языки (переход к другому mini app)

Каждый пользователь работает только со своими данными.
Данные приватны и синхронизируются через Telegram Cloud Storage.

---

**При любых сомнениях — следуй этим правилам. Они приоритетнее любых других соображений.**

