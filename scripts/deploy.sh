#!/bin/bash

# Цвета для вывода
GREEN='\033[0;32m'
RED='\033[0;31m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}🚀 Начинаю деплой...${NC}\n"

# Проверяем статус git
echo -e "${BLUE}📋 Проверяю статус репозитория...${NC}"
git status --short

# Проверяем есть ли изменения
if git diff-index --quiet HEAD --; then
  echo -e "${YELLOW}⚠️  Нет изменений для коммита${NC}"
  
  # Проверяем есть ли что пушить
  LOCAL=$(git rev-parse @)
  REMOTE=$(git rev-parse @{u} 2>/dev/null || echo "")
  
  if [ -z "$REMOTE" ]; then
    echo -e "${YELLOW}⚠️  Удаленный репозиторий не настроен${NC}"
    exit 0
  fi
  
  if [ "$LOCAL" = "$REMOTE" ]; then
    echo -e "${GREEN}✅ Все изменения уже запушены${NC}"
    echo -e "${BLUE}📦 Netlify автоматически задеплоит при следующем коммите${NC}"
    exit 0
  else
    echo -e "${BLUE}📤 Есть коммиты для отправки, пушим...${NC}"
    git push
    if [ $? -eq 0 ]; then
      echo -e "${GREEN}✅ Успешно запушено в GitHub${NC}"
      echo -e "${BLUE}📦 Netlify начнет деплой автоматически...${NC}"
    else
      echo -e "${RED}❌ Ошибка при push в GitHub${NC}"
      exit 1
    fi
    exit 0
  fi
fi

# Добавляем все изменения
echo -e "\n${BLUE}➕ Добавляю изменения в staging...${NC}"
git add .

if [ $? -ne 0 ]; then
  echo -e "${RED}❌ Ошибка при добавлении файлов${NC}"
  exit 1
fi

# Показываем что будет закоммичено
echo -e "${BLUE}📝 Изменения для коммита:${NC}"
git status --short

# Создаем коммит
COMMIT_MSG="Update: $(date +'%Y-%m-%d %H:%M')"
echo -e "\n${BLUE}💾 Создаю коммит: ${COMMIT_MSG}${NC}"
git commit -m "$COMMIT_MSG"

if [ $? -ne 0 ]; then
  echo -e "${RED}❌ Ошибка при создании коммита${NC}"
  exit 1
fi

COMMIT_HASH=$(git rev-parse --short HEAD)
echo -e "${GREEN}✅ Коммит создан: ${COMMIT_HASH}${NC}"

# Показываем статистику коммита
echo -e "\n${BLUE}📊 Статистика коммита:${NC}"
git show --stat --oneline HEAD

# Пушим в GitHub
echo -e "\n${BLUE}📤 Отправляю в GitHub...${NC}"
git push

if [ $? -eq 0 ]; then
  echo -e "\n${GREEN}✅ Успешно запушено в GitHub${NC}"
  echo -e "${GREEN}📦 Netlify автоматически начнет деплой...${NC}"
  echo -e "\n${BLUE}🔗 Проверьте статус деплоя на Netlify${NC}"
  echo -e "${BLUE}   Обычно деплой занимает 1-2 минуты${NC}"
  
  # Пытаемся определить URL проекта (если есть remote)
  REMOTE_URL=$(git remote get-url origin 2>/dev/null || echo "")
  if [[ $REMOTE_URL == *"github.com"* ]]; then
    REPO=$(echo $REMOTE_URL | sed -E 's/.*github.com[:/]([^/]+\/[^/]+)(\.git)?$/\1/')
    echo -e "\n${BLUE}📋 Репозиторий: https://github.com/${REPO}${NC}"
  fi
else
  echo -e "\n${RED}❌ Ошибка при push в GitHub${NC}"
  echo -e "${RED}   Проверьте подключение к интернету и права доступа${NC}"
  exit 1
fi

echo -e "\n${GREEN}✨ Деплой инициирован успешно!${NC}"

